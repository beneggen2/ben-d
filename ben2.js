package- open -of (url)
-open -WebSocket ()
-open -WebTransport ()
{
  -fetch -ip ()
  get- Geolocation () 
   fetch -open -port (https -port)
    if_false- goto ;var we


}
(we) ={
   



}