 open () -Option -FileList()
 {
   PublicKeyCredential ()= fetch -root -File()
    open- AnalyserNode (File_import )
     debugger ;root -File()
      _import () -WebSocket ()
      -importScripts -WebSocket()
     }
     open- new seed -key  
     open_seed- key ()
     
    encode-root () -- 
   encodeURI in (129-B )
   {
    get - encode  -key () ;Storage- Location (url) 
    

   }     


 